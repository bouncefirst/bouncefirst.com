# Local reference only — old site scrape, not part of the published site
/bouncefirst.com/

# Local-only working directories (notes, drafts, future plans pages, etc.)
/docs/
/plans/

# macOS / editor cruft
.DS_Store
**/.DS_Store

# Design handoff reference — never commit
/design_handoff_portfolio/
